function (context, args){
	var fs = #s.scripts.fullsec();
	var i, x, log = [];

	for(i = 0; i < fs.length; ++i){
		for(x = 0; x < npc.length; ++x){
				log.push(fs[i]);
	}}}
	return log;
};
